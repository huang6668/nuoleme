# Compatibility Note

This repository now uses `AGENTS.md` as the primary instruction file for Codex-style agents.

If you are a tool that previously read `CLAUDE.md`, continue by reading `AGENTS.md` in the repository root and treat it as the source of truth.
